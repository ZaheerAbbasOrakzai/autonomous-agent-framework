global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "anomaly-monitor"
    static_configs:
      - targets: ["host.docker.internal:9001"]
        labels:
          service: anomaly-monitor
