/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Allow the dev server to be reached from outside the container
  experimental: {
    // No special experiments needed for this project
  },
};

module.exports = nextConfig;
